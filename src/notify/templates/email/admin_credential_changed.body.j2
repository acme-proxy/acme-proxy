{% if change == "password" -%}
The password for your web-admin account "{{ username }}" was changed.
{%- elif change == "second_factor_enabled" -%}
A second factor was enabled on your web-admin account "{{ username }}".
{%- elif change == "second_factor_disabled" -%}
The second factor on your web-admin account "{{ username }}" was removed.
{%- else -%}
The recovery codes for your web-admin account "{{ username }}" were regenerated;
the previous set no longer works.
{%- endif %}
{% if by_self -%}
This change was made from your own session.
{%- else -%}
This change was made by another administrator, not from your session.
{%- endif %}

Time: {{ at }} (epoch seconds)
{%- if client_ip %}
Client IP: {{ client_ip }}
{%- endif %}
{%- if user_agent %}
User agent: {{ user_agent }}
{%- endif %}

If you did not expect this, sign in and review your account, or ask another
administrator to disable it.
